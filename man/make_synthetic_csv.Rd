% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lib_synthetic.R
\name{make_synthetic_csv}
\alias{make_synthetic_csv}
\title{Generate a Synthetic CSV From a Schema Recipe}
\usage{
make_synthetic_csv(schema, out_path, n_rows = NULL, seed = NULL)
}
\arguments{
\item{schema}{The synthetic recipe (a list with \code{columns}, and optional
\code{n_rows}, \code{seed}, and \code{row_replication}).}

\item{out_path}{Path where the CSV is written.}

\item{n_rows}{Number of rows (persons, if replicating) to generate.
Defaults to \code{schema$n_rows}, then 50000.}

\item{seed}{Random seed for reproducibility. Defaults to \code{schema$seed},
then 91735246.}
}
\value{
Invisibly, a list with \code{path}, \code{rows} (rows written), and
\code{seed} used.
}
\description{
Generates a reproducible synthetic data set from the
\code{schema$synthetic_recipe} block of a provenance object and writes it to
a CSV. Columns are produced in declaration order so later columns can
reference earlier ones, a shared per-row latent propensity drives any
Bernoulli columns, and an optional row-replication block expands
per-person rows.
}
