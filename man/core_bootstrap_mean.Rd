% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core_stats.R
\name{core_bootstrap_mean}
\alias{core_bootstrap_mean}
\title{Bootstrap replicate means (C backend)}
\usage{
core_bootstrap_mean(x, B = 1000L, seed = 42L)
}
\arguments{
\item{x}{Numeric vector to resample.}

\item{B}{Number of bootstrap replicates (default 1000).}

\item{seed}{Seed for the core's generator (default 42).}
}
\value{
A numeric vector of length \code{B}: the replicate means.
}
\description{
\code{B} resamples of \code{x}, drawn with replacement and each the same length
as \code{x}, with the mean of every resample returned. The resampling uses
the core's own 64-bit Mersenne Twister seeded by \code{seed}, NOT R's RNG,
so a given \code{seed} reproduces the same replicates in every binding of
the core and R's own random stream is left untouched.
}
\examples{
set.seed(1)
x <- stats::rnorm(50, mean = 5)

reps <- core_bootstrap_mean(x, B = 500, seed = 7)
length(reps)

# The replicates centre on the sample mean, and their spread estimates
# the standard error.
c(sample = mean(x), bootstrap = mean(reps))
c(bootstrap_se = stats::sd(reps), formula_se = stats::sd(x) / sqrt(length(x)))

# A percentile confidence interval for the mean.
stats::quantile(reps, c(0.025, 0.975))

# Reproducible: the same seed gives the same replicates, and R's own
# random stream is not consumed.
identical(core_bootstrap_mean(x, 100, seed = 1),
          core_bootstrap_mean(x, 100, seed = 1))
}
