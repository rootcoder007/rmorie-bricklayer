% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core_stats.R
\name{core_normal_logpdf}
\alias{core_normal_logpdf}
\title{Normal log-density (C backend)}
\usage{
core_normal_logpdf(x, mean = 0, sd = 1)
}
\arguments{
\item{x}{Numeric vector of quantiles.}

\item{mean}{Distribution mean (length-1, default 0).}

\item{sd}{Distribution standard deviation (length-1, default 1, > 0).}
}
\value{
A numeric vector the length of \code{x}. Equivalent to
\code{stats::dnorm(x, mean, sd, log = TRUE)}.
}
\description{
The logarithm of the normal density, computed directly rather than as
\code{log(dnorm(x))}, so it stays finite far into the tails where the
density itself underflows to zero.
}
\examples{
core_normal_logpdf(c(-1, 0, 1))

# Identical to stats::dnorm(log = TRUE).
all.equal(core_normal_logpdf(-2:2, 0.3, 1.7),
          stats::dnorm(-2:2, 0.3, 1.7, log = TRUE))

# Still finite where the density itself underflows to zero.
stats::dnorm(50)                  # 0
core_normal_logpdf(50)            # about -1251
}
