% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/digest_extra.R
\name{rmbl_file_digest}
\alias{rmbl_file_digest}
\alias{sha512_file}
\alias{crc32_file}
\title{SHA-512 and CRC-32 of a file}
\usage{
sha512_file(path, block_bytes = 1048576L)

crc32_file(path, block_bytes = 1048576L)
}
\arguments{
\item{path}{Path to an existing file.}

\item{block_bytes}{Read size in bytes (default 1048576). Affects speed
only, never the result.}
}
\value{
A length-1 character vector (\code{sha512_file()}) or numeric
(\code{crc32_file()}).
}
\description{
Streams the file in blocks, so memory use does not grow with the file.
The SHA-256 counterpart is \code{\link[=sha256_file]{sha256_file()}}.
}
\examples{
p <- tempfile()
writeLines("capsule payload", p)

sha512_file(p)
crc32_file(p)

# The block size is a speed knob and cannot change the digest.
identical(sha512_file(p, 16), sha512_file(p, 1048576))

unlink(p)
}
