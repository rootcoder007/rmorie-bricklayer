% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/categorical_guard.R
\name{verify_recode}
\alias{verify_recode}
\title{Prove a recode with a before/after cross-tabulation}
\usage{
verify_recode(original, recoded, declared)
}
\arguments{
\item{original, recoded}{Parallel vectors (before and after).}

\item{declared}{Named character vector: the mapping the analyst claims was
applied. Identity is assumed for old values absent from \code{declared}.}
}
\value{
Invisibly, the cross-tabulation as a data frame, if and only if
every check passes.
}
\description{
Cross-tabulates the original against the recoded values and errors unless
the realized mapping is a function (each old category to exactly one new
category) that matches the declared mapping, with no rows lost and no
change in missingness.
}
\examples{
verify_recode(
  c("W", "B", "W"), c("White", "Black", "White"),
  c(W = "White", B = "Black")
)
}
