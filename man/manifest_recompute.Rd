% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/repro.R
\name{manifest_recompute}
\alias{manifest_recompute}
\title{Recompute a manifest's recorded statistics against the data}
\usage{
manifest_recompute(manifest, data, statistics, tol = NULL)
}
\arguments{
\item{manifest}{A manifest, as from
\code{\link[=make_manifest]{make_manifest()}} and
\code{\link[=record]{record()}}.}

\item{data}{The data the statistics are computed from.}

\item{statistics}{Named list of functions of
\code{data}. Names must match the recorded result names.}

\item{tol}{Optional numeric tolerance overriding each
result's own recorded \code{tol}. Supply it to check more strictly than
the original run did.}
}
\value{
A list of class \code{bricklayer_recompute}: \code{ok}, a
\code{results} data frame with one row per recomputed statistic (
\code{name}, \code{recorded}, \code{recomputed}, \code{delta},
\code{tol}, \code{status}) , and \code{unchecked}, the names recorded
but not recomputed.
}
\description{
Re-evaluates named statistics against \code{data} and compares each to
what the manifest recorded. This is the check
\code{\link[=verify_capsule]{verify_capsule()}} cannot make: that one
confirms the record is internally consistent, which an
edited-at-the-time manifest also is.
}
\details{
Every recorded result that was NOT recomputed is reported too, as
\code{unchecked}. An analysis that recorded twenty statistics and
re-derives three has seventeen it has not re-derived, and a report that
quietly omitted them would read as a clean bill of health.
}
\examples{
d <- data.frame(x = 1:10)
m <- make_manifest(list(dataset = "demo"), environment = FALSE)
m <- record(m, "mean_x", observed = mean(d$x), expected = 5.5)
m <- record(m, "n", observed = nrow(d), expected = 10)

# recomputing both reproduces them
res <- manifest_recompute(m, d, list(mean_x = function(z) mean(z$x),
                                     n = function(z) nrow(z)))
res$ok
res$results[, c("name", "recorded", "recomputed", "status")]

# recomputing one leaves the other reported as unchecked
manifest_recompute(m, d, list(n = function(z) nrow(z)))$unchecked

# and data that no longer matches the record is caught
manifest_recompute(m, data.frame(x = 1:11),
                   list(n = function(z) nrow(z)))$ok
}
\seealso{
\code{\link[=verify_capsule]{verify_capsule()}},
\code{\link[=capsule_falsify]{capsule_falsify()}},
\code{\link[=record]{record()}}.
}
