% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rule_lib.R
\name{rmbl_rule_library}
\alias{rmbl_rule_library}
\alias{rule_in_set}
\alias{rule_between}
\alias{rule_not_null}
\alias{rule_unique}
\alias{rule_regex}
\alias{rule_increasing}
\alias{rule_within_n_mads}
\alias{rule_complete_rows}
\alias{rule_distinct_rows}
\alias{rule_col_count}
\title{Ready-made validation rules}
\usage{
rule_in_set(column, set, na_pass = TRUE, severity = c("warning", "fatal"))

rule_between(column, lo, hi, na_pass = TRUE, severity = c("warning", "fatal"))

rule_not_null(column, severity = c("warning", "fatal"))

rule_unique(column, severity = c("warning", "fatal"))

rule_regex(column, pattern, na_pass = TRUE, severity = c("warning", "fatal"))

rule_increasing(column, strictly = FALSE, severity = c("warning", "fatal"))

rule_within_n_mads(
  column,
  n = 3,
  na_pass = TRUE,
  severity = c("warning", "fatal")
)

rule_complete_rows(severity = c("warning", "fatal"))

rule_distinct_rows(columns = NULL, severity = c("warning", "fatal"))

rule_col_count(n, severity = c("warning", "fatal"))
}
\arguments{
\item{column}{Column the rule applies to.}

\item{set}{Allowed values (\code{rule_in_set}).}

\item{na_pass}{Treat \code{NA} as passing (default \code{TRUE}; see above).}

\item{severity}{\code{"warning"} (default) or \code{"fatal"}.}

\item{lo, hi}{Inclusive bounds (\code{rule_between}).}

\item{pattern}{Regular expression the values must match
(\code{rule_regex}).}

\item{strictly}{Require a strict increase rather than non-decreasing
(\code{rule_increasing}).}

\item{n}{Multiplier for \code{rule_within_n_mads}, or the expected count
for \code{rule_col_count}.}

\item{columns}{Columns that jointly must be unique
(\code{rule_distinct_rows}), or \code{NULL} for all of them.}
}
\value{
A \code{\link[=rule]{rule()}} object.
}
\description{
Constructors for the checks most schemas need, each returning a
\code{\link[=rule]{rule()}} that \code{\link[=validate_rules]{validate_rules()}} evaluates. Use \code{\link[=rule]{rule()}} directly for
anything not covered here.
}
\details{
\code{NA} handling is explicit and per-rule, because the right answer
differs. \code{rule_not_null()} exists precisely to fail on \code{NA}. The
value rules (\code{rule_in_set}, \code{rule_between}, \code{rule_regex},
\code{rule_within_n_mads}) treat \code{NA} as PASSING by default, so that a
column's missingness is reported once by \code{rule_not_null()} or
\code{max_missing_fraction} rather than again by every other rule; set
\code{na_pass = FALSE} to make them fail on it instead.
}
\examples{
df <- data.frame(
  id = c(1, 2, 2),
  grade = c("a", "b", "z"),
  score = c(5, 200, 7),
  email = c("a@b.com", "nope", "c@d.org"),
  day = c(3, 1, 2),
  stringsAsFactors = FALSE
)

rules <- list(
  rule_unique("id", severity = "fatal"),
  rule_in_set("grade", c("a", "b", "c")),
  rule_between("score", 0, 100),
  rule_regex("email", "^[^@]+@[^@]+\\\\\\\\.[a-z]+$"),
  rule_increasing("day")
)
names(validate_rules(df, rules))

# Each names the rows that failed.
validate_rules(df, rules)$grade_in_set$rows

# Clean data passes every one of them.
ok <- data.frame(id = 1:3, grade = c("a", "b", "c"),
                 score = c(5, 50, 7),
                 email = c("a@b.com", "c@d.org", "e@f.net"),
                 day = 1:3, stringsAsFactors = FALSE)
length(validate_rules(ok, rules))

# A robust outlier rule: MADs from the median, not standard
# deviations from the mean, so one wild value cannot hide the others.
validate_rules(data.frame(v = c(1, 2, 3, 2, 1, 900)),
               rule_within_n_mads("v", 5))$v_within_mads$rows

# Whole-table rules.
validate_rules(df, rule_distinct_rows())
validate_rules(df, rule_col_count(5))
validate_rules(df, rule_complete_rows())
}
\seealso{
\code{\link[=rule]{rule()}} for an arbitrary predicate, \code{\link[=validate_rules]{validate_rules()}} to
evaluate them, \code{\link[=infer_schema]{infer_schema()}} for the structural checks that need
no rules at all.
}
