% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lib_data_loader.R
\name{load_provenance}
\alias{load_provenance}
\title{Load a Pinned Data-Provenance Record}
\usage{
load_provenance(path)
}
\arguments{
\item{path}{Path to the provenance JSON file.}
}
\value{
The parsed provenance as a nested list (via
\code{\link[jsonlite:fromJSON]{jsonlite::fromJSON()}} with \code{simplifyVector = FALSE}), or \code{NULL} if
the file does not exist.
}
\description{
Reads a \code{data_provenance.json} file describing a project's pinned data
source: the CKAN endpoint, resource name pattern, expected SHA256,
Wayback snapshot, schema, and synthetic-data recipe.
}
\examples{
prov_file <- tempfile(fileext = ".json")
writeLines('{"dataset": {"title": "demo"}, "sha256": "abc"}', prov_file)
prov <- load_provenance(prov_file)
prov$dataset$title
load_provenance(file.path(tempdir(), "no-such-file.json"))  # NULL
}
