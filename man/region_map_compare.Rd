% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/region_map.R
\name{region_map_compare}
\alias{region_map_compare}
\title{Compare a recomputed region map against a published one}
\usage{
region_map_compare(published, observed, unit, cols = NULL)
}
\arguments{
\item{published}{The region map as published.}

\item{observed}{The region map as recomputed.}

\item{unit}{Name of the unit identifier column, present in both.}

\item{cols}{Columns to compare. Defaults to every column the two share
apart from \code{unit}.}
}
\value{
A data frame, one row per compared column plus a \code{rows} row for
units present on one side only: \code{column}, \code{cells}, \code{mismatched} and
\code{first}, the first disagreement written out as text. Zero
\code{mismatched} throughout is the passing result.
}
\description{
Matches two region maps on the unit identifier and compares the named
columns cell by cell.
}
\details{
Numeric columns are compared with \code{\link[=all.equal]{all.equal()}} at its default
tolerance, so a coordinate that survived a round trip through text is
not reported as a change; everything else is compared exactly after
trimming whitespace.

What this establishes is that the recomputation reproduced the
published assignment. It does NOT establish that the assignment is
right: run the same method against the same boundary file and a
definitional error reproduces perfectly. That is what
\code{\link[=region_map_second_route]{region_map_second_route()}} is for.
}
\examples{
pub <- data.frame(inst = c("North Jail", "South Jail"),
                  cd = c("3557", "3520"), stringsAsFactors = FALSE)
obs <- pub
region_map_compare(pub, obs, "inst")

# a changed assignment is reported with the unit that moved
obs$cd[2] <- "3521"
region_map_compare(pub, obs, "inst")
}
\seealso{
\code{\link[=region_map_second_route]{region_map_second_route()}},
\code{\link[=region_map_integrity]{region_map_integrity()}}
}
