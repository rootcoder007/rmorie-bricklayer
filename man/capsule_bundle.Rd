% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bundle.R
\name{capsule_bundle}
\alias{capsule_bundle}
\alias{capsule_bundle_read}
\alias{capsule_bundle_verify}
\title{Bundle a capsule into one signed artifact}
\usage{
capsule_bundle(
  dir,
  manifest,
  key,
  files = NULL,
  context = NULL,
  prehash = "none",
  note = NULL,
  path = NULL
)

capsule_bundle_read(path)

capsule_bundle_verify(bundle, dir, manifest = NULL, key_expected = NULL)
}
\arguments{
\item{dir}{Directory the capsule lives in.}

\item{manifest}{A manifest, as from
\code{\link[=make_manifest]{make_manifest()}}.}

\item{key}{A signing key from
\code{\link[=fips_keygen]{fips_keygen()}} or
\code{\link[=pqc_keygen]{pqc_keygen()}}.}

\item{files}{Paths relative to \code{dir}. Defaults to
every regular file in \code{dir}, excluding the bundle itself.}

\item{context, prehash, note}{As in
\code{\link[=capsule_attest]{capsule_attest()}}.}

\item{path}{Where to write the bundle. Defaults to
\code{capsule_bundle.json} inside \code{dir}.}

\item{bundle}{A bundle read back with
\code{capsule_bundle_read()}, or the path to one.}

\item{key_expected}{Optional public key hex the
bundle's attestation must carry. Supply it when you know which key
should have signed: without it the check confirms the bundle is
internally consistent, which any key's holder could arrange.}
}
\value{
\code{capsule_bundle()} the bundle, invisibly, with the path it
was written to as an attribute; \code{capsule_bundle_read()} the bundle;
\code{capsule_bundle_verify()} a list with \code{ok} and a \code{checks}
data frame.
}
\description{
Records a digest of every file named, the manifest's canonical digest,
and an attestation covering both, as a single JSON file.
\code{\link[=capsule_bundle_verify]{capsule_bundle_verify()}} re-hashes
the files on disk and checks everything against it.
}
\details{
The file digests are inside the signed payload, not alongside it. That
is the whole point: a list of hashes that is not itself signed can be
rewritten to match whatever the files now say, and a recipient who
re-hashes the files and compares them to that list learns only that the
list is consistent with itself.

What it establishes: the named files have not changed since signing, the
manifest has not changed, and both were signed together by the holder of
that key. What it does not: that the key belongs to anyone in
particular, or that the manifest's claims are true.
}
\examples{
dir <- tempfile()
dir.create(dir)
write.csv(data.frame(x = 1:3), file.path(dir, "data.csv"),
          row.names = FALSE)
m <- make_manifest(list(dataset = "demo"), environment = FALSE)
key <- fips_keygen("ML-DSA-44")

b <- capsule_bundle(dir, m, key, note = "as published")
capsule_bundle_verify(attr(b, "path"), dir, manifest = m)$ok

# touch a byte of the data and the bundle no longer holds
write.csv(data.frame(x = 1:4), file.path(dir, "data.csv"),
          row.names = FALSE)
capsule_bundle_verify(attr(b, "path"), dir, manifest = m)$ok
unlink(dir, recursive = TRUE)
}
\seealso{
\code{\link[=capsule_attest]{capsule_attest()}},
\code{\link[=verify_capsule]{verify_capsule()}},
\code{\link[=manifest_digest]{manifest_digest()}}.
}
