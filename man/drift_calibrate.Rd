% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scan_adjust.R
\name{drift_calibrate}
\alias{drift_calibrate}
\title{Calibrate the drift screens on data known not to have drifted}
\usage{
drift_calibrate(data, n = 50L, alpha = 0.01, seed = 1L, ...)
}
\arguments{
\item{data}{The capsule's data frame.}

\item{n}{Number of random half-splits.}

\item{alpha}{Significance level passed to \code{\link[=capsule_drift]{capsule_drift()}}.}

\item{seed}{Seed for the splits.}

\item{...}{Passed to \code{\link[=capsule_drift]{capsule_drift()}}.}
}
\value{
A list of class \code{rmbl_drift_calibration}: \code{columns} (per
column, the share of splits on which it was flagged), \code{any_flag}
(share of splits with at least one flag), \code{alpha}, \code{n}, and
\code{alpha_familywise} (the per-screen level that would hold the
family-wise false-alarm rate at \code{alpha}, Bonferroni).
}
\description{
\code{\link[=capsule_drift]{capsule_drift()}} flags a column when a
Kolmogorov-Smirnov, chi-square
or population-stability screen fires. How often that happens when
nothing has changed is the false-alarm rate of the screen on this
capsule, and it depends on the columns, their sizes and \code{alpha}. This
estimates it by splitting the same data at random into two halves
\code{n} times and running the screens on the halves, which is the null of
"a re-fetch of identical data". A capsule whose screens fire on 30\% of
identical re-fetches needs a smaller \code{alpha} or fewer screened
columns before a drift verdict means anything.
}
\examples{
set.seed(1)
d <- data.frame(a = rnorm(400), b = sample(letters[1:4], 400, TRUE))
drift_calibrate(d, n = 10)
}
