% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sign.R
\name{fips_mu}
\alias{fips_mu}
\alias{fips_sign_mu}
\alias{fips_verify_mu}
\title{Sign an ML-DSA message digest computed elsewhere}
\usage{
fips_mu(key, message, context = NULL, prehash = "none")

fips_sign_mu(key, mu, deterministic = FALSE)

fips_verify_mu(key, mu, signature)
}
\arguments{
\item{key}{A key from
\code{\link[=fips_keygen]{fips_keygen()}} or
\code{\link[=fips_key]{fips_key()}}. An ML-DSA scheme: SLH-DSA has no
external-mu interface, because its digest depends on per-signature
randomness that the signer chooses.}

\item{message}{Length-1 character vector or raw vector.}

\item{context}{Optional context string, as in
\code{\link[=capsule_sign]{capsule_sign()}}.}

\item{prehash}{Pre-hash function, as in
\code{\link[=capsule_sign]{capsule_sign()}}.}

\item{mu}{The 64 raw bytes from \code{fips_mu()}.}

\item{deterministic}{As in
\code{\link[=capsule_sign]{capsule_sign()}}.}

\item{signature}{A signature from \code{fips_sign_mu()}
or \code{\link[=capsule_sign]{capsule_sign()}}.}
}
\value{
\code{fips_mu()} returns 64 raw bytes; \code{fips_sign_mu()} a
\code{bricklayer_signature}; \code{fips_verify_mu()} a length-1 logical.
}
\description{
The external-mu interface. \code{fips_mu()} reduces a message to the
64-byte value mu that is the only thing ML-DSA signing actually
consumes; \code{fips_sign_mu()} signs that value and
\code{fips_verify_mu()} checks it.
}
\details{
The point is that the message need never reach the key. A large file can
be reduced to mu on the machine that holds it and only mu handed to
whatever holds the signing key -- a smartcard, a remote signer, another
process. mu is not a bare digest: it binds the public key (through
\code{tr = H(pk)}) and the context string, so a mu computed under one
key cannot be signed under another to any useful effect.

The resulting signature is an ordinary ML-DSA signature.
\code{\link[=capsule_verify]{capsule_verify()}} accepts it, given the
same message and context.
}
\examples{
key <- fips_keygen("ML-DSA-65")
mu <- fips_mu(key, "a manifest digest", context = "release")
length(mu)

sig <- fips_sign_mu(key, mu)
fips_verify_mu(key, mu, sig)

# the same signature verifies the ordinary way, from the message
capsule_verify("a manifest digest", sig, key, context = "release")

# mu is computable from the PUBLIC key alone, which is what lets the
# message stay on the machine that has it
identical(fips_mu(fips_public_key(key), "a manifest digest",
                  context = "release"), mu)
}
\references{
National Institute of Standards and Technology (2024).
Module-Lattice-Based Digital Signature Standard. FIPS 204.
\doi{10.6028/NIST.FIPS.204}
}
\seealso{
\code{\link[=capsule_sign]{capsule_sign()}},
\code{\link[=fips_keygen]{fips_keygen()}}.
}
