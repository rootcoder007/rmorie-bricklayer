% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/digest_extra.R
\name{chunk_file}
\alias{chunk_file}
\title{Split a file into fixed-size chunks}
\usage{
chunk_file(path, chunk_bytes = 1048576L)
}
\arguments{
\item{path}{Path to an existing file.}

\item{chunk_bytes}{Chunk size in bytes (default 1048576, i.e. 1 MiB).}
}
\value{
A character vector of chunks, in file order. A zero-length
file gives \code{character(0)}.
}
\description{
Reads \code{path} as bytes and returns them as chunk strings suitable for
\code{\link[=merkle_root]{merkle_root()}} and friends. The default 1 MiB chunk is a compromise:
smaller chunks localise a change more precisely but make the tree and
its proofs larger.
}
\examples{
p <- tempfile()
writeLines(rep("some capsule content", 50), p)

# One small chunk size to show the splitting.
ch <- chunk_file(p, chunk_bytes = 128)
length(ch)

# The chunks reconstruct the file and pin it as a Merkle root.
merkle_root(ch)

# Editing the file changes exactly one leaf.
unlink(p)
}
\seealso{
\code{\link[=merkle_root]{merkle_root()}}, \code{\link[=sha512_file]{sha512_file()}}
}
