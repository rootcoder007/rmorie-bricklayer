% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kem.R
\name{kem_sizes}
\alias{kem_sizes}
\title{Byte lengths of an ML-KEM parameter set}
\usage{
kem_sizes(level)
}
\arguments{
\item{level}{Security level: 512, 768 or 1024.}
}
\value{
A named integer vector: \code{encapsulation_key},
\code{decapsulation_key}, \code{ciphertext}, \code{seed} and
\code{shared_secret}, all in bytes.
}
\description{
Reports the sizes FIPS 203 fixes, so a caller never has to hard-code
them.
}
\examples{
kem_sizes(768)

# the shared secret is 32 bytes at every level: the level buys
# security margin, not a longer secret
vapply(c(512, 768, 1024),
       function(l) kem_sizes(l)[["shared_secret"]], integer(1))
}
\seealso{
\code{\link[=kem_keygen]{kem_keygen()}}.
}
