% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rate.R
\name{rate}
\alias{rate}
\alias{rate.data.frame}
\alias{rate.default}
\title{Event rates per unit of population}
\usage{
rate(x, ...)

\method{rate}{data.frame}(
  x,
  count,
  population,
  by = NULL,
  per = 1000,
  conf_level = 0.95,
  min_count = 0,
  ...
)

\method{rate}{default}(x, population, per = 1000, conf_level = 0.95, min_count = 0, ...)
}
\arguments{
\item{x}{A data frame, or a numeric vector of counts.}

\item{...}{Passed to methods.}

\item{count}{Column of counts: non-negative whole numbers.}

\item{population}{Column of exposure: positive. Person-years, stops,
residents -- whatever the events were at risk of happening to.}

\item{by}{Character vector of grouping columns. The rate is computed
within each group.}

\item{per}{The rate denominator: a positive number, or \code{"1k"},
\code{"10k"}, \code{"100k"}, \code{"1m"}. Default 1000.}

\item{conf_level}{Confidence level for the interval.}

\item{min_count}{Counts at or below this are flagged as too small to
report, with the rate still computed. Default 0, which flags
nothing; published guidance often uses 5 or 10.}
}
\value{
A data frame of class \code{rmbl_rate} with the grouping columns,
\code{count}, \code{population}, \code{rate}, \code{lower}, \code{upper} and \code{flag}, and a
\code{rate} attribute recording the denominator and confidence level.
}
\description{
Counts divided by exposure and scaled to a denominator, with the
exact Poisson interval that says how much of the result is signal.
}
\details{
A count is not comparable across areas of different size or years of
different population; a rate is. What a rate does not do is become
reliable just because it is a rate: three events in a small area
gives a rate with an interval several times its own width, and the
interval here is the one that says so. It is the exact Poisson
interval, computed through the gamma relation, so a count of zero has
a lower limit of exactly zero rather than a negative number.

\code{per} takes a number, or one of \code{"1k"}, \code{"10k"}, \code{"100k"}, \code{"1m"},
because a denominator mistyped by a factor of ten is invisible once
it reaches a table.
}
\examples{
stops <- data.frame(
  division = c("North", "South", "East"),
  stops = c(412, 77, 3),
  residents = c(120000, 41000, 9500))

# per 1,000 residents, the default
rate(stops, stops, residents, by = "division")

# the denominator published guidance usually asks for
rate(stops, stops, residents, by = "division", per = "100k")

# East's three events: the interval is wider than the estimate, and
# flagging it is the point of min_count
rate(stops, stops, residents, by = "division", per = "100k",
     min_count = 5)

# vectors work too, for a single figure
rate(3, 9500, per = "100k")
}
\seealso{
\code{\link[=share]{share()}} for a percentage of a total, \code{\link[=rate_change]{rate_change()}} for
the change in a rate between periods, \code{\link[=sir]{sir()}} for a rate compared
against an expected count rather than a population.
}
