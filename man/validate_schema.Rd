% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lib_data_loader.R
\name{validate_schema}
\alias{validate_schema}
\title{Validate a Data Frame Against a Provenance Schema}
\usage{
validate_schema(df_raw, provenance)
}
\arguments{
\item{df_raw}{The data frame to validate.}

\item{provenance}{A provenance list as returned by \code{\link[=load_provenance]{load_provenance()}}.
The \code{schema} block may contain \code{expected_columns},
\code{structural_invariants} (\code{min_data_rows}, \code{max_data_rows}), and
\code{expected_value_sets} (a named list of allowed values per column).}
}
\value{
A named list of issues; each issue is a list with \code{severity}
(\code{"fatal"} or \code{"warning"}) and a human-readable \code{message}. A
zero-length list means the data frame is clean.
}
\description{
Checks a raw data frame against the \code{schema} block of a provenance
object: required columns, row-count bounds, and allowed categorical
value sets. Returns the issues found rather than raising, so the caller
decides how to react.
}
\examples{
prov <- list(schema = list(
  expected_columns      = c("id", "year"),
  structural_invariants = list(min_data_rows = 1),
  expected_value_sets   = list(year = 2020:2025)
))
df <- data.frame(id = 1:3, year = c(2020, 2021, 2030))
issues <- validate_schema(df, prov)
names(issues)  # flags the out-of-set year value
}
