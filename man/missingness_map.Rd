% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eda2.R
\name{missingness_map}
\alias{missingness_map}
\title{Text map of where the missing values are}
\usage{
missingness_map(data, height = 20L, width = 12L)
}
\arguments{
\item{data}{A data frame.}

\item{height}{Maximum rows in the map (default 20).}

\item{width}{Maximum characters per column label (default 12).}
}
\value{
A character vector of the map's lines, invisibly; printed as a
side effect.
}
\description{
Draws the missingness of a whole table as a grid, one character per
cell block -- a console counterpart of \code{visdat::vis_miss()} that needs
no graphics device, so it works over SSH, in a log, and inside a
capsule's plain-text summary.
}
\details{
Rows are binned so the map fits \code{height} lines; a block is drawn at
the shade its missing proportion falls in. Seeing the table at once is
the point: a diagonal band, a block of rows, or a single ragged column
are all instantly recognisable shapes that a column of percentages
is not.
}
\examples{
set.seed(1)
df <- data.frame(
  complete = 1:100,
  block = c(rep(NA, 30), 31:100),
  scattered = ifelse(stats::runif(100) < 0.3, NA, 1),
  mostly_gone = c(1:10, rep(NA, 90))
)
missingness_map(df)
}
\seealso{
\code{\link[=missingness_pattern]{missingness_pattern()}}, \code{\link[=missing_runs]{missing_runs()}}
}
