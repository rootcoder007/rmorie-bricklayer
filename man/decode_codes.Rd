% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/categorical_guard.R
\name{decode_codes}
\alias{decode_codes}
\title{Decode numeric category codes with an explicit code-to-label dictionary}
\usage{
decode_codes(x, value_labels, keep_na = TRUE)
}
\arguments{
\item{x}{Numeric or character vector of codes.}

\item{value_labels}{Named character vector:
\code{c("1" = "White", "2" = "Black", ...)}
with the codes as names.}

\item{keep_na}{Logical; \code{NA} codes stay \code{NA} (default) rather than error.}
}
\value{
A character vector of labels with a \code{recode_audit} attribute.
}
\description{
The guard for data that arrives from SPSS, Stata or SAS as integer codes:
every code observed must be listed in \code{value_labels}, and the result is
the label, never the code. A code without a label is an error, because
treating codes as categories (or \code{as.numeric()} on a factor of them) is
how groups get renumbered.
}
\examples{
decode_codes(c(1, 2, 2, 3),
             c("1" = "White", "2" = "Black", "3" = "Indigenous"))
}
