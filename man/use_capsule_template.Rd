% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/capsule_template.R
\name{use_capsule_template}
\alias{use_capsule_template}
\title{Start a capsule from a template}
\usage{
use_capsule_template(path, name = basename(path), example = FALSE)
}
\arguments{
\item{path}{Directory to create.}

\item{name}{Capsule name.}

\item{example}{Use the shipped OTIS table as the source so the
skeleton runs immediately.}
}
\value{
The directory path, invisibly.
}
\description{
Writes the skeleton of a reproducible capsule: a \code{data_provenance.json}
with the fields \code{\link[=load_provenance]{load_provenance()}} and
\code{\link[=verify_capsule]{verify_capsule()}} expect, an
\code{analysis.R} that fetches the source, verifies it, runs
\code{\link[=analyse_table]{analyse_table()}} and writes the manifest and report,
and a README
that says how to run it. Every field that must be filled in is marked
\code{TODO}. The script runs as written against the shipped OTIS example
when \code{example = TRUE}.
}
\examples{
d <- use_capsule_template(tempfile("capsule-"), example = TRUE)
list.files(d)
}
