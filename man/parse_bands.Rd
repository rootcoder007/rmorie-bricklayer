% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bands.R
\name{parse_bands}
\alias{parse_bands}
\title{Parse banded category labels into numeric bounds}
\usage{
parse_bands(x, closed_upper = TRUE, integer_scale = TRUE)
}
\arguments{
\item{x}{Character labels.}

\item{closed_upper}{For an open upper band, whether
the stated number is included. \code{"50+"} includes 50;
\code{"Greater than 15"} does not, so its lower bound is 16 for integer
data. Controlled per-label by the wording, and this argument only
settles the ambiguous \code{"+"} form.}

\item{integer_scale}{Whether the quantity is
integer-valued, which decides whether \code{"greater than 15"} starts at
16 or just above 15.}
}
\value{
A data frame with \code{label}, \code{lower}, \code{upper},
\code{open_lower} and \code{open_upper}. An unparseable label gives
\code{NA} bounds rather than a guess.
}
\description{
Recognises the forms that open-data publishers actually use: a bare
number ( \code{"3"}) , a closed range ( \code{"18 to 24"},
\code{"18-24"}, \code{"18 - 24"}) , an open upper band ( \code{"50+"},
\code{"Greater than 15"}, \code{"over 15"}, \code{"more than 15"},
\code{"65 and over"}) , and an open lower band ( \code{"<18"},
\code{"under 18"}, \code{"less than 18"}) .
}
\examples{
parse_bands(c("1", "2 to 5", "6 to 10", "Greater than 10"))

parse_bands(c("18 to 24", "25 to 49", "50+"))

# An unrecognised label is reported as unparsed, not guessed at.
parse_bands(c("3", "unknown", "not stated"))
}
\references{
The forms recognised here are taken from the categories the Ontario
Ministry of the Solicitor General publishes in its inmate datasets (the
segregation and restrictive-confinement releases), where the
placement-count and age categories are banded and the top band is open.
}
\seealso{
\code{\link[=band_values]{band_values()}},
\code{\link[=band_sensitivity]{band_sensitivity()}}
}
