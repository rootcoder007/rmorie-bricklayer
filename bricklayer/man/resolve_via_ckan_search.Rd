% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lib_data_loader.R
\name{resolve_via_ckan_search}
\alias{resolve_via_ckan_search}
\title{Resolve a Download URL via CKAN package_search}
\usage{
resolve_via_ckan_search(provenance)
}
\arguments{
\item{provenance}{A provenance list as returned by \code{\link[=load_provenance]{load_provenance()}}.
Uses \code{resource$search_query}, \code{resource$name_match_pattern},
\code{resource$format}, and \code{dataset$ckan_api_endpoint}.}
}
\value{
The matched resource URL as a character string, or \code{NULL} if no
query or base URL can be derived, the request fails, or nothing
matches.
}
\description{
Fallback for \code{\link[=resolve_via_ckan]{resolve_via_ckan()}} when the dataset slug has changed.
Derives the CKAN portal base URL from the provenance's \code{package_show}
endpoint, runs a \code{package_search} query (from \code{resource$search_query},
or derived from the name-match pattern), and returns the URL of the
first matching resource, preferring CSV format when specified.
}
