# {{project_title}}

**Reproducibility bundle for:** *{{paper_title}}*
**Author:** {{author}} · {{contact}} · ORCID {{orcid}}
**Generated by:** morie-bricklayer v{{bricklayer_version}} on {{build_date}}

---

## How to run

| OS | What to do |
|---|---|
| **macOS** | Double-click `START_HERE.command`. If Gatekeeper blocks: right-click → Open → Open. |
| **Windows** | Double-click `START_HERE.bat`. If SmartScreen blocks: *More info* → *Run anyway*. |
| **Linux** | Open terminal in this folder and run `./start_here.sh`. |
| **Any OS (power user)** | `Rscript setup_and_run.R --quick --data /path/to/file` |

**Only requirement:** R 4.0+ from https://cran.r-project.org/

---

## Data source

This bundle uses: **{{resource_name}}**, published by {{publisher}}.

- **Catalogue page:** {{catalogue_url}}
- **Licence:** {{licence_name}} ({{licence_url}})
- **Direct file URL:** {{direct_url}}
- **SHA256 (pinned at build time):** `{{sha256}}`

If the bundle includes the CSV vendored, that copy is what was retrieved and verified at build time. Otherwise the script will offer to download it via the CKAN API (with SHA256 verification) or via your own manual browser download.

---

## What you get

A folder named `results_YYYYMMDD-HHMMSS/` appears next to the launcher containing CSV outputs, a `manifest.json` audit trail (expected vs observed for every cross-check), and a `SUMMARY.txt` in plain language explaining what was done and where every file lives.

Expected on a clean run:

- **Real data:** {{expected_pass_real}} of {{expected_total}} PASS, 0 DIFFER
- **Synthetic data:** 0 PASS, 0 DIFFER, {{expected_total}} INFO (synthetic mode forces INFO)

---

## Security & trust

See `SECURITY.md` for full integrity verification instructions, including the SHA256 hash of the bundle itself for out-of-band verification.

---

## Contact

Questions, bug reports, or "the bundle won't run" notes:
> **{{author}}**
> {{contact}}
> ORCID {{orcid}}

---

*Licence: {{licence}} (scripts) · {{data_licence}} (data)*
*Generated by [morie-bricklayer](https://github.com/morie-oss/morie-bricklayer).*
