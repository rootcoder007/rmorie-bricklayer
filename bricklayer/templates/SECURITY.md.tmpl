# SECURITY & TRUST — {{project_title}}

> *"Don't open `.tar.gz` files, they contain malware and viruses."*
> — a misconception worth addressing directly.

This document explains what this bundle does, what it touches, what it doesn't touch, and how you can verify everything yourself.

---

## TL;DR

**Every file in this bundle is plaintext source code.** No binaries, no compiled executables. Open any of them in any text editor. The scripts run R, bash, and Windows batch — three of the most heavily-audited script languages on the planet.

---

## Archive format ≠ malware

`.zip`, `.tar.gz`, `.7z` — they're all just **containers for files**. A safe text file is safe in any container. A malicious file is malicious in any container. The actual security questions are:

1. **What does the bundle CONTAIN?** ← *plaintext source code only*
2. **What does that code DO when you run it?** ← *answered below*
3. **Where did the bundle COME FROM, and how do you know it wasn't tampered with?** ← *SHA256 verification below*
4. **Who is responsible for it?** ← *{{author}}, named publisher with ORCID*

---

## What the scripts do — every action, named

### File system
- **Read** their own folder (to find sibling scripts and the data file).
- **Write** to ONE of: the bundle folder (default), `~/Downloads`, `~/Documents`, R's OS-canonical app-data folder, or a folder you type yourself.
- **Never write** outside the locations you choose. **Never write** to system directories. **Never request** admin/root/sudo rights.

### Network
The scripts contact exactly these public endpoints:

{{network_endpoints_table}}

No telemetry. No analytics. No third-party services. No personal data leaves your machine.

### Process spawning
- `Rscript` (which you installed yourself) runs the analysis. Nothing else is spawned.

### Things the scripts do NOT do
- Do not modify shell config (`.bashrc`, `.zshrc`, PowerShell profile).
- Do not install system packages (`brew`, `apt`, `dnf`, `winget`, `pip`, `npm`).
- Do not register services, launch agents, scheduled tasks, or cron jobs.
- Do not touch the keychain, credential manager, or password store.
- Do not access your browser, email, contacts, calendar, photos, microphone, camera, or location.
- Do not require, request, or use admin/root/sudo privileges anywhere, ever.

---

## How to verify the bundle wasn't tampered with in transit

Compute the SHA256 of what you received:

```bash
# macOS / Linux
shasum -a 256 {{bundle_filename}}
```

```powershell
# Windows
Get-FileHash {{bundle_filename}} -Algorithm SHA256
```

Compare against the SHA256 sent out-of-band (separate email, Signal message, video call):

```
{{bundle_sha256}}
```

If the hashes match, the file is byte-for-byte identical to what was packaged. If they differ, **do not trust the file** — contact the publisher.

---

## How to audit the code in 15 minutes

1. Unzip the bundle.
2. Open `setup_and_run.R` in any text editor. Read the labelled sections.
3. Open `analysis.R` (your project's R script).
4. Open `START_HERE.command` (or `.bat` / `.sh`) — each is ~110 lines, just invokes `Rscript`.
5. Grep for the usual red flags:
   ```
   grep -nE 'system|shell|exec|eval|curl|wget|chmod|sudo|rm |unlink' *.sh *.R
   ```

---

## Publisher identity

- **Name:** {{author}}
- **Email:** {{contact}}
- **ORCID:** {{orcid}}
- **Affiliation:** {{affiliation}}

If you have any doubts about provenance, you can contact me directly.

---

## Licences

| Component | Licence |
|---|---|
| Bundle scripts (`setup_and_run.R`, launchers, helpers) | {{licence}} |
| Project analysis script (`analysis.R`) | {{licence}} |
| Source dataset (if vendored) | {{data_licence}} |
| Synthetic data (if you use synthetic mode) | Public domain |

---

*Generated by [morie-bricklayer](https://github.com/morie-oss/morie-bricklayer) v{{bricklayer_version}}.*
